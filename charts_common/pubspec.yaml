name: charts_common
version: 0.12.0
description: A common library for charting packages.
homepage: https://github.com/google/charts

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  collection: ^1.19.1
  intl: ">=0.15.2 < 0.18.0"
  logging: ^1.3.0
  meta: ^1.1.1
  vector_math: ^2.2.0

dev_dependencies:
  mockito: ^5.6.1
  test: ^1.28.0
